<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:background="?android:attr/windowBackground"
        android:gravity="center_horizontal">

    <!-- Moon image - großer zentraler Fokus -->
    <ImageView
            android:id="@+id/image_phase"
            android:layout_width="280dp"
            android:layout_height="280dp"
            android:layout_marginTop="48dp"
            android:scaleType="fitCenter"
            app:srcCompat="@drawable/phase_100"/>

    <!-- Mondphasen-Name -->
    <TextView
            android:id="@+id/text_phase"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text=""
            android:textSize="22sp"
            android:textColor="?attr/colorOnBackground"
            android:letterSpacing="0.1"
            android:fontFamily="serif"/>

    <!-- Prozentwert -->
    <TextView
            android:id="@+id/text_phase_percent"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:text="100%"
            android:textSize="14sp"
            android:textColor="?attr/colorOnBackground"
            android:alpha="0.5"/>

    <!-- Spacer -->
    <View
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_weight="1"/>

    <!-- Divider -->
    <View
            android:layout_width="match_parent"
            android:layout_height="0.5dp"
            android:background="?attr/colorOnBackground"
            android:alpha="0.15"
            android:layout_marginHorizontal="24dp"/>

    <!-- Bottom info strip -->
    <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="72dp"
            android:orientation="horizontal"
            android:weightSum="3">

        <!-- Mondzeichen -->
        <LinearLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center">
            <TextView
                    android:id="@+id/label_moon"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="☽"
                    android:textSize="11sp"
                    android:textColor="?attr/colorOnBackground"
                    android:alpha="0.4"/>
            <TextView
                    android:id="@+id/text_moon_sign"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text=""
                    android:textSize="13sp"
                    android:fontFamily="serif"
                    android:textColor="?attr/colorOnBackground"/>
        </LinearLayout>

        <!-- Sonnenzeichen -->
        <LinearLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center">
            <TextView
                    android:id="@+id/label_sun"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="☀"
                    android:textSize="11sp"
                    android:textColor="?attr/colorOnBackground"
                    android:alpha="0.4"/>
            <TextView
                    android:id="@+id/text_sun_sign"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text=""
                    android:textSize="13sp"
                    android:fontFamily="serif"
                    android:textColor="?attr/colorOnBackground"/>
        </LinearLayout>

        <!-- KIN -->
        <LinearLayout
                android:layout_width="0dp"
                android:layout_height="match_parent"
                android:layout_weight="1"
                android:orientation="vertical"
                android:gravity="center">
            <TextView
                    android:id="@+id/label_kin"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="✦"
                    android:textSize="11sp"
                    android:textColor="?attr/colorOnBackground"
                    android:alpha="0.4"/>
            <TextView
                    android:id="@+id/text_kin"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:layout_marginTop="4dp"
                    android:text="—"
                    android:textSize="13sp"
                    android:fontFamily="serif"
                    android:textColor="?attr/colorOnBackground"/>
        </LinearLayout>

    </LinearLayout>

</LinearLayout>
